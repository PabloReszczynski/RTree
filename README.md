# Una implementacion de RTree

## Como correr
Después de clonar:
  `git submodule init`
  
Para compilar:
`cmake .` `make`

Para correr el ejecutable
`./src/exe`

Para correr los tests
`./tests/runUnitTests`

